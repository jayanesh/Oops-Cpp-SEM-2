/*Create a text processing utility that searches for words and replaces them throughout a
paragraph.*/

#include<iostream>
using namespace std;
int main(){
    string para;
    string key;
    cout<<"Enter your paragraph: ";
    getline(cin,para);
    cout<<"Enter the word you want to replace: ";
    cin>>key;
    
}